# Dotenv fixture. Values are intentionally non-sensitive.
APP_MODE=development
PUBLIC_ORIGIN=https://example.invalid
FEATURE_PREVIEW=true
RETRY_COUNT=3
GREETING="hello world"
COMBINED=${APP_MODE}-${FEATURE_PREVIEW}
